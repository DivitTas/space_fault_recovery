# Training-only dependencies
# Do NOT add these to pyproject.toml — they are not needed for the server/Space runtime.
# Install with: pip install -r training/requirements-train.txt

torch>=2.1.0
transformers>=4.40.0
trl>=0.9.0
peft>=0.10.0
bitsandbytes>=0.43.0
accelerate>=0.28.0
datasets>=2.18.0
wandb>=0.16.0
matplotlib>=3.8.0
